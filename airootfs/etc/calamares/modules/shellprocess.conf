- name: "zotixos-preinstall"
  command: "/usr/local/bin/zotix-pre-install.sh"
  timeout: 120
  environment:
    FS: "${partition.filesystem}"

- name: "mirrors"
  script: "/usr/local/bin/zotix-mirrors.sh"
  timeout: 180
  environment:
    COUNTRY: "${locale.region}"

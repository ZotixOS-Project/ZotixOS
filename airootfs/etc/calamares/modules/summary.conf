style: "list"

groups:
  - title: "User"
    lines:
      - "Username: {users.username}"
      - "Hostname: {users.hostname}"

  - title: "System"
    lines:
      - "Locale: {locale.region}"
      - "Timezone: {locale.timezone}"
